class search:
    def __init__(self, words: list[str]):
        self.words = words 

    def sort(self, input: str) -> list[str]:
        '''
        sorts data word list based on input, sorted by match % to give the highest matching words.
        '''
        words = self.words 

        for i in words:
            pass

    def match(self, input:str, word:str) -> float:
        pass 

